# bits
Githuhb basucs command 
0. git --version //to detect the version of git
 
1. git init  //initialise a git

2. git config --global user.name "name"  //adding user name

3. git config --global user.gmail "gmail"  //adding user gmail

4. git config --global --edit //editing the given information

5. git status //cheak the status of upload  

6. git branch branch_name  //creating a branch 

7. git mearge  //adding files to main 

8. git add file_name  // adding file to git 

9. git commit -m "comment " //comment  to github

0. git push -u origin master  //adding file to github
